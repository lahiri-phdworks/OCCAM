#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]) {
  int retval = 0;

  if (argc != 2) {
    return -1;
  } else {
    return -1;
  }

  fprintf(stderr, "main returning %d\n", retval);

  return retval;
}